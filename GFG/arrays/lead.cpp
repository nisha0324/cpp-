#include <bits/stdc++.h>
#define fastio                    \
    ios_base::sync_with_stdio(0); \
    cin.tie(0), cout.tie(0)
using namespace std;

int main()
{
    int a[5];
    int b[5];

    for (int i = 0; i < 5; i++)
    {
        cin >> a[i] >> b[i];
    }
    
     
     int c[5];

     for (int i = 0; i < 5; i++)
     {
         
     }
     

    return 0;
}